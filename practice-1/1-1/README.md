Задание 1-1: Системные вызовы
Требования:
•Используйте только системные вызовы: open(), read(), write(), close()
•НЕ используйте: fopen(), fprintf(), fscanf()
•Обрабатывайте ошибки с помощью perror()
•Компиляция: gcc -Wall -Wextra -o task task.c

Вариант 6: Создать файл words.txt со словами (20+ по строкам), подсчитать слова,
записать в wordcount.txt

Создается файл words.txt в который записывается 25 слов.
Затем из файла words.txt считывается весь текст, происходит подсчет слов.
Количество слов записывается в файл wordcount.txt.

Для запуска используется ./Task
В итоговом файле написано Количество слов: 25
