Задание 1-1: Системные вызовы<br>
Требования:<br>
•Используйте только системные вызовы: open(), read(), write(), close()<br>
•НЕ используйте: fopen(), fprintf(), fscanf()<br>
•Обрабатывайте ошибки с помощью perror()<br>
•Компиляция: gcc -Wall -Wextra -o task task.c<br>

Вариант 6: Создать файл words.txt со словами (20+ по строкам), подсчитать слова,<br>
записать в wordcount.txt<br>

Создается файл words.txt в который записывается 25 слов.<br>
Затем из файла words.txt считывается весь текст, происходит подсчет слов.<br>
Количество слов записывается в файл wordcount.txt.<br>

Для запуска используется ./task<br>
В итоговом файле написано Количество слов: 25
